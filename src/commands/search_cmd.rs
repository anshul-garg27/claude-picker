//! `claude-picker search` — full-text search across every session.
//!
//! Day-2 work. Stub prints a pointer to the existing Python implementation.

pub fn run() -> anyhow::Result<()> {
    eprintln!("claude-picker search — coming in Day 2.");
    eprintln!("For now, use: python3 lib/session-search.py <query>");
    Ok(())
}
