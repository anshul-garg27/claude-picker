//! `claude-picker diff` — side-by-side diff of two sessions.
//!
//! Day-2 work. Stub prints a pointer to the existing Python implementation.

pub fn run() -> anyhow::Result<()> {
    eprintln!("claude-picker diff — coming in Day 2.");
    eprintln!("For now, use: python3 lib/session-diff.py <id1> <id2>");
    Ok(())
}
