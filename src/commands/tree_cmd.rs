//! `claude-picker tree` — session fork tree.
//!
//! Day-2. Stub prints a pointer to the existing Python implementation until
//! the Rust port lands.

pub fn run() -> anyhow::Result<()> {
    eprintln!("claude-picker tree — coming in Day 2.");
    eprintln!("For now, use: python3 lib/session-tree.py");
    Ok(())
}
